name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    uses: PerAsperaMods/.github/.github/workflows/release-mod.yml@main
    with:
      mod-id: WaterWork
      mod-type: yaml
